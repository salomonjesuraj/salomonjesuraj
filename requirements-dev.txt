# Core developer/test tooling.
#
# Keep this lightweight: normal unit tests should not require the heavy
# sentiment-engine runtime stack (torch/transformers). Install service packages
# in editable mode with `make setup-dev` after this file is installed.
pytest>=9.1
pytest-asyncio>=1.4
ruff>=0.16
mypy>=2.3

# Runtime libraries imported by the core editable packages used in tests.
aiohttp>=3.9
asyncpg>=0.29
httpx>=0.27
msgpack>=1.0
pydantic>=2.7
pydantic-settings>=2.2
redis[hiredis]>=5.0
structlog>=24.1
protobuf>=4.25
tzdata>=2025.2
