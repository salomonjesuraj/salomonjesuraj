[project]
name = "infusion-alerter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["infusion-models", "infusion-streams", "infusion-common", "httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/alerter"]
